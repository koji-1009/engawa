# Per-namespace command specs

One normative file per namespace (`window.md`, `fs.md`, …) detailing each command's args, results, and error codes. Each file is written in the **same commit** as its conformance test and implementation (CLAUDE.md commit discipline).
