# hosts/linux — clean-room, empty until contract freeze

C++ (gtkmm) + WebKitGTK (webkit2gtk-4.1). **Empty until `contract-1.0` is tagged.** Implemented clean-room from `spec/` + `shell-js/` + `conformance/` + adapter `spec.md`s only — the `hosts/macos/` source is off-limits (CLAUDE.md clean-room rule). If blocked, the block is a spec hole: fix `spec/`, then proceed. Definition of done: the conformance suite + `make notes` on Linux.
