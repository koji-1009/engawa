#pragma once
// The one contract-version constant. engineInfo.contractVersion MUST equal engawa.contractVersion
// (§1.1); both come from here. Bumped only when the contract's negotiated version moves.
inline constexpr const char* kContractVersion = "0.1.0";
inline constexpr const char* kPlatform = "windows";
inline constexpr const char* kHostVersion = "engawa-windows-0.1";
